# file-upload-proj
Проект загрузки файлов на сервер для собеседования на работу.

Разбор ошибок проведён в конце файла.

## Описание
Проект выполнен на Node.js версии 26.2.0.

Ветка итогового результата проекта - `main`.

## Окружение
Файлы `.env` **могут** присутствовать в каждом подпроекте внутри packages: `API` и `Web`. В общем и целом, для каждого параметра `.env` в коде прописаны начальные значения в классах program-data.ts соответствующего подпроекта, поэтому файлы .env не необходимы.

Примеры `.env` (указаны начальные значения, прописанные в коде):
* `API`:
    ``` bash
    API_PORT="4001" # Отвечает за запуск сервера на указанном порте
    UPL_DIR="uploads" # Отвечает за сохранение бинарников в указанной директории
    META_DIR="data" # Отвечает за сохранение метаданных в указанную директорию
    META_FILE="files.json" # Отвечает за сохранение метаданных в указанный файл
    ```
* `Web`:
    ``` bash
    NEXT_PUBLIC_API_URL="http://localhost:4001" # Отвечает за обращение клиента к указанному API
    ```

В каждом подпроекте существует файл `.env.example`. На их основе можно создавать необходимые файлы `.env`.

## Запуск
1. Для установки необходимых зависимостей проекта выполняется команда:
    ``` bash
    npm install
    ```
2. Для запуска обеих частей единовременно:
    ``` bash
    npm run dev # С помощью установленного пакета concurrently запускает оба проекта (через скрипты server и client)
    ```
3. Для запуска серверной части отдельно:
    ``` bash
    npm run server # Выполняет скрипт внутри подпроекта API, который в свою очередь запускает nodemon (запуск сервера)
    ```
4. Для запуска клиентской части отдельно:
    ``` bash
    npm run client # Выполняет скрипт внутри подпроекта Web, который в свою очередь запускает next
    ```

## Комментарий
Проект очень понравился, выполнял с удовольствием. Ранее не сталкивался с сохранением бинарников на диск сервера, переданных через запрос.

К сожалению, потрачено много времени на то, чтобы разобраться с философией Node.js и синтаксисом самого языка TypeScript и его компонентом middleware multer. Однако после ознакомления с вышеперечисленным, работа с Next.js и React прошла заметно быстрее.

Также на скорость выполнения повлияла подготовка к сессии и проблемы с впн.

При наличии дополнительного времени - потратил бы его на внедрение Rate limit, на оформление css компонентов React и фильтр по MIME.

## Разбор ошибок
### 1. README
Исправлено. Случайно проглядел приставку `API_` в переменной `PORT`, когда описывал проект, прошу прощения. :)

Добавлены файлы `.env.example` в соответствующие подпроекты.

### 2. npm run dev
Исправлено. Подумал, что нужно выбрать один из вариантов. С `concurrently` получилось даже удобнее, не знал.

Добавлен скрипт `dev` в корневом `package.json`.

### 3. DELETE 404 без JSON
Исправлено. Неправильно интерпретировал формулировку задания.

### 4. DELETE: сначала JSON, потом файл - риск «осиротевшего» файла
Исправлено. Поменял порядок: сначала удаляется файл, если не получилось - обработка ошибки.

### 5. UI: после delete total считается неверно
Исправлено.

### 6. UI: delete без проверки ответа API
Исправлено. Правда исправление несколько странное по отношению к ошибке 404, поскольку даже если запись о файле отсутствует - всё равно лучше карточку удалить. Тем не менее, добавлена функция alert для предупреждения о возникнувшей ошибке.

### 7. UI: PDF пытается показаться как изображение
Исправлено. Принято решение перенаправлять пользователя на адрес API для загрузки pdf по нажатию на ссылку.

### 8. Загрузка: успех при любом статусе кроме 400
Исправлено.

### 9. Прочее
* Исправлено. Думал, что во всех клиентских компонентах необходимо прописывать директиву 'use client'. Теперь понял, что layout.tsx - это серверный компонент в Next.js.
* Исправлено.
* Исправлено.
* Исправлено "responce". Однако не было найдено "Frontent" в файлах.